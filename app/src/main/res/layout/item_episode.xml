<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="pList"
            type="com.huawei.podcast.data.model.PodCastList" />
        <variable
            name="clickInterface"
            type="com.huawei.podcast.utils.ClickListener" />

    </data>

<androidx.constraintlayout.widget.ConstraintLayout
    android:onClick="@{() -> clickInterface.onItemClick(pList)}"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/txt_date_size"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="@dimen/twenty"
        android:text="@{pList.name}"
        android:layout_marginStart="@dimen/twenty"
        app:layout_constraintTop_toTopOf="parent"
        style="@style/normal_black_text"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/txt_title"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="@dimen/ten"
        android:text="@{pList.email}"
        android:layout_marginStart="@dimen/twenty"
        app:layout_constraintTop_toBottomOf="@+id/txt_date_size"
        style="@style/bold_text"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/txt_duration"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="@dimen/ten"
        android:text="@{pList.avatar}"
        android:layout_marginStart="@dimen/twenty"
        app:layout_constraintTop_toBottomOf="@+id/txt_title"
        style="@style/normal_black_text"/>
    <ImageView
        android:id="@+id/img_download"
        android:layout_width="@dimen/thirty"
        android:layout_height="@dimen/thirty"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="@dimen/thirty"
        android:layout_marginEnd="@dimen/twenty"
        android:src="@drawable/ic_download_black"
        app:layout_constraintEnd_toEndOf="parent"/>
    <View
        android:layout_marginTop="@dimen/twenty"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_gravity="center_horizontal"
        app:layout_constraintTop_toBottomOf="@+id/txt_duration"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="@dimen/twenty"
        android:layout_marginStart="@dimen/twenty"
        android:background="@color/colorPrimary"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>